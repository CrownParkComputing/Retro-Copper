<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Hardware feature declarations for Play Store device filtering -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- Android TV support (required="false" = same APK for phone + TV) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- Bluetooth is optional - used for game controllers -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- MANAGE_EXTERNAL_STORAGE (all-files access) is deliberately NOT
         requested. It is a Play "sensitive permission": shipping it means
         filling in a declaration form and passing a review that Google
         normally only grants to file managers, backup and antivirus apps, and
         an undeclared one blocks the release outright. Instead the user
         grants one folder through the Storage Access Framework picker and
         its contents are copied into the app's own media folder, where the
         native core's POSIX file APIs can read them without any permission.
         See MediaFolderAccess. -->

    <!-- Bluetooth controller support (Steam Controller BLE, HID devices) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Haptic feedback for game controllers -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT_TREE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="content" />
        </intent>
    </queries>

    <application
        android:label="Retro-Copper"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:allowBackup="true"
        android:hasFragileUserData="true"
        android:largeHeap="true">

        <!-- Flutter launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android TV launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SDL emulator activity, launched by the Flutter UI over a MethodChannel.
             Runs in its own process so a crash in the core cannot take the UI down. -->
        <activity android:name="com.uae4arm2026.Uae4ArmEmulatorActivity"
            android:process=":sdl"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|navigation"
            android:theme="@style/EmulatorTheme"
            android:enableOnBackInvokedCallback="true"
            android:exported="true">
            <meta-data android:name="SDL_MAIN_LIBRARY_NAMES" android:value="uae4arm" />
        </activity>

        <!-- Controller mapping screen -->
        <activity android:name="com.uae4arm2026.ControllerMapActivity"
            android:process=":sdl"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
